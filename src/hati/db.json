{
	"default_prod_db": "localhost:test",
	"default_cli_db" : "localhost:test",

	"db_profiles": {
		"localhost": {
			"host": "localhost",
			"username": "root",
			"password": "",
			"db": ["test"]
		}
	}
}
